package scripts.application

plugins {
    id("scripts.config.properties")
    id("scripts.application.compiling")
    id("scripts.application.sources")
    id("scripts.application.publishing")
    id("scripts.application.signing")
}